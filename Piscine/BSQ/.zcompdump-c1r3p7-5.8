Fonctions :
	Main(argc, argv) -> Appeler toutes les fonctions / return 0
	Size(argv[1]) -> Trouver le nombre de lignes du plateau / return int
	Find_char(argv[1]) -> Cherche les 3 characteres / return char[3 characteres]
	Find_square(argv[1]) -> Cherche le plus grand carre possible sinon retourne le plus en haut et a gauche / return int[longueur et largeur]
	
